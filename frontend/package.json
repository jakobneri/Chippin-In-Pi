{
  "name": "chippin-in-pi-frontend",
  "version": "1.0.0",
  "description": "Frontend for Chippin-In-Pi",
  "scripts": {
    "dev": "echo 'Angular not yet initialized. See frontend/README.md for setup instructions.' && exit 1",
    "build": "echo 'Angular not yet initialized. See frontend/README.md for setup instructions.' && exit 1"
  }
}
